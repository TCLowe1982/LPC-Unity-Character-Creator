# This repo IS a Unity package (not a Unity project), so .meta files MUST be committed.
# Ignore only OS/editor cruft and any local test-host artifacts.

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# If a test Unity project is ever nested here, ignore its generated folders
**/Library/
**/Temp/
**/Obj/
**/Logs/
**/UserSettings/
*.csproj
*.sln

# Beads/Dolt local DB internals are tracked via .beads/*.jsonl, ignore the rest if present
.beads/dolt/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Offline test bridge: Tests~ is hidden from Unity by the trailing '~'. Track its sources
# (the *~ and *.csproj rules above would otherwise drop them); bin/ and obj/ stay ignored
# via Tests~/.gitignore.
!Tests~/
!Tests~/**/*.cs
!Tests~/**/*.csproj
!Tests~/**/*.md
!Tests~/.gitignore
